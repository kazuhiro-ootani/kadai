%h1 タスク一覧


.task-list
  .task-title
    = @toptask.title
  .task-content
    = @toptask.content
  .task-footer
.task-new
  ←New!

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @tasks.each do |task|
      %tr
        %td= task.title
        %td= link_to 'Show', task
        %td= link_to 'Edit', edit_task_path(task)
        %td= link_to 'Destroy', task, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'タスク新規作成', new_task_path

= link_to 'ログアウト', account_session_path(current_user.id), method: :delete if current_user
